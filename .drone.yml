pipeline:
  build:
    image: golang
    commands:
      - go get
      - go build
      - go test

services:
  postgres:
    image: postgres
    environment:
      - POSTGRES_USER=gorest
      - POSTGRES_PASSWORD=gorest
      - POSTGRES_DB=gorest
