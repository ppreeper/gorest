pipeline:
    build:
        image: golang
        commands:
            - go version
            - go get github.com/gorilla/mux

service:
    postgres:
        image: postgres:9.4.5
        environment:
            - POSTGRES_USER=myapp
